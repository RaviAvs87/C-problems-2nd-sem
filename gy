#include<stdio.h>

int main() {
    int v1[3] = {2, 3, 5};
    int v2[3] = {5, 6, 7};
    int dot = 0;

    for(int i = 0; i < 3; i++) {
        dot = dot + (v1[i] * v2[i]);
    }

    printf("Dot Product = %d", dot);

    return 0;
}